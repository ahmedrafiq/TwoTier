function menu_dim(which_item) {   which_item.className = which_item.className.replace(/\bhilite\b/,'dim');}function menu_hilite(which_item) {	which_item.className = which_item.className.replace(/\bdim\b/,'hilite');}function category_dim(which_item) {   which_item.className = which_item.className.replace(/\bcategory_hilite\b/,'category_dim');}function category_hilite(which_item) {	which_item.className = which_item.className.replace(/\bcategory_dim\b/,'category_hilite');}function pager_dim(which_item) {   which_item.className = which_item.className.replace(/\bpager_hilite\b/,'pager_dim');}function pager_hilite(which_item) {	which_item.className = which_item.className.replace(/\bpager_dim\b/,'pager_hilite');}function button_dim(which_button) {   which_button.className = which_button.className.replace(/\bdown\b/,'up');}function button_hilite(which_button) {	which_button.className = which_button.className.replace(/\bup\b/,'down');}function date_set(which) {	var theyear 	= (which.form.date_year.value >= 1900)  ? which.form.date_year.value  : '0000' ;	var themonth 	= (which.form.date_month.value > 0) ? which.form.date_month.value : '00' ;	var theday 		= (which.form.date_day.value > 0)   ? which.form.date_day.value   : '00' ;	which.form.custom_date.value = theyear + "-" + themonth + "-" + theday;}function custom_date_set(which_field) {	var the_year_selector = document.getElementById(which_field+'_year');	var the_month_selector = document.getElementById(which_field+'_month');	var the_day_selector = document.getElementById(which_field+'_day');	var the_custom_field = document.getElementById(which_field);	var theyear 	= (the_year_selector.value >= 1900)  ? the_year_selector.value  : '0000' ;	var themonth 	= (the_month_selector.value > 0) ? the_month_selector.value : '00' ;	var theday 		= (the_day_selector.value > 0)   ? the_day_selector.value   : '00' ;	the_custom_field.value = theyear + "-" + themonth + "-" + theday;}function new_change(which) {	which.className = which.className + " changed";}function match_new_passwords(which, alert_message) {	if (which.form.new_password.value != which.form.new_password2.value) {		alert(alert_message);		which.form.new_password.value  = "";		which.form.new_password2.value = "";	} else {		which.form.new_password.className = which.form.new_password.className + " changed";	}}function left_nav_dim(which_item) {   which_item.className = which_item.className.replace(/\bleft_nav_hilite\b/,'');}function left_nav_hilite(which_item) {	which_item.className = which_item.className += ' left_nav_hilite';}function footer_dim(which_item) {   which_item.className = which_item.className.replace(/\bfooter_hilite\b/,'footer_dim');}function footer_hilite(which_item) {	which_item.className = which_item.className.replace(/\bfooter_dim\b/,'footer_hilite');}function reward_dim(which_item) {   which_item.className = which_item.className.replace(/\breward_hilite\b/,'reward_allowed');}function reward_hilite(which_item) {	which_item.className = which_item.className.replace(/\breward_allowed\b/,'reward_hilite');}